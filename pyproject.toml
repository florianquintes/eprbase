[project]
name = "eprbase"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "florianquintes", email = "53559005+florianquintes@users.noreply.github.com" }
]
requires-python = ">=3.8"
dependencies = []

[project.scripts]
eprbase = "eprbase:main"

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
